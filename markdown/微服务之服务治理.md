# 微服务之流量治理

## 熔断、降级和限流

微服务的出现是为了应对多变的需求和频繁的发布，这种场景下任何一个微服务的时时可靠都是不太可能的，禁止故障的传播，避免雪崩效应是非常必要的。最直接的手段是`熔断`，熔断对有问题的服务的调用，即快速返回错误的响应。其次，为了减少微服务调用发生的错误对用户体验的影响，做`降级`而不是直接跟用户返回一串异常信息是更好的。同时，各个服务实际都有容量上限，高并发或者瞬时高并发，为了保证系统的稳定性、可用性，系统以牺牲部分请求为代价或者延迟处理请求为代价，保证系统服务整体可用，这种行为我们称为`限流`。