Elasticsearch
	原理
		倒排索引
	核心概念
		文档(Document)  ：一条数据
		字段(Field)     ：数据里面的json字段
		索引(Index)     ：表
		映射(Mapping)   ：表结构约束
	安装
	Restful Api
		索引
			创建索引库：PUT /{索引库名}
			查询索引库：GET /{索引库名}
			删除索引库：DELETE /{索引库名}
			修改索引库（只能添加字段）：PUT /{索引库名}/_mapping
		文档
			创建文档：POST /{索引库名}/_doc/文档id
			查询文档：GET /{索引库名}/_doc/文档id
			删除文档：DELETE /{索引库名}/_doc/文档id
			全量修改：PUT /{索引库名}/_doc/文档id
			增量修改：POST /{索引库名}/_update/文档id {"doc":{字段}}
	java连接方式：
		1、REST Client 
		2、Jest 
		3、Spring Data 
		4、Spring Data Elasticsearch Repositories
	部署方式
	数据同步方式：
		1、cdc（binlog）
		2、mq消息 业务层推送消息
		3、定时批处理同步（Logstash）

https://www.cnblogs.com/sexintercourse/p/18646560
