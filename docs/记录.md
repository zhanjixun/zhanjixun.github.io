### git配置用户名和密码

```shell
#设置git用户名 提交代码时候显示这个名称
git config --global user.name  "zhanjixun"  
#设置git邮箱 提交代码时候显示这个邮箱
git config --global user.email  "zhanjixun@qq.com"

#设置保存密码，不用每次输入 ssh下使用则不用设置此项
git config --global credential.helper store
```

