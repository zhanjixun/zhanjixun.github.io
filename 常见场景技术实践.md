# 常见技术场景实践

本文记录常见的技术场景的解决方案，记录总结，以便查阅。

## 字符串操作



## 集合操作

### 判空

### 交集、并集和差集

```java
//com.google.common.collect.Sets

Set<Integer> sets1 = Sets.newHashSet(1, 2, 3, 4, 5);
Set<Integer> sets2 = Sets.newHashSet(4, 5, 6, 7, 8, 9);
//交集 [4, 5]
System.out.println(Sets.intersection(sets1, sets2));

//并集 set1+set2  [1, 2, 3, 4, 5, 6, 7, 8, 9]
System.out.println(Sets.union(sets1, sets2));

//差集 set1-set2  [1, 2, 3]
System.out.println(Sets.difference(sets1, sets2));
```

## 定时任务

### xxl-job



## Excel操作

### poi



### EasyExcel

github主页：[https://github.com/alibaba/easyexcel](https://github.com/alibaba/easyexcel)

maven：[https://mvnrepository.com/artifact/com.alibaba/easyexcel](https://mvnrepository.com/artifact/com.alibaba/easyexcel)

```xml
<dependency>
    <groupId>com.alibaba</groupId>
    <artifactId>easyexcel</artifactId>
    <version>2.0.0</version>
</dependency>
```

#### 读Excel

```java
@Test
public void simpleRead() {
    SyncReadListener readListener = new SyncReadListener();
    EasyExcel.read("写出的Excel.xlsx", DemoData.class, readListener).sheet().doRead();
    List<Object> list = readListener.getList();
}
```

提供几个`ReadListener`

```java
/**
 * 异步解析
 *
 * @author :zhanjixun
 * @date : 2020/04/27 21:00
 */
public class ASyncReadListener<T> extends AnalysisEventListener<T> {

    private BiConsumer<T, AnalysisContext> consumer;

    public ASyncReadListener(BiConsumer<T, AnalysisContext> consumer) {
        this.consumer = consumer;
    }

    @Override
    public void invoke(T object, AnalysisContext context) {
        consumer.accept(object, context);
    }


    @Override
    public void doAfterAllAnalysed(AnalysisContext context) {
    
    }

}

/**
 * 异步分批解析
 *
 * @author :zhanjixun
 * @date : 2020/04/27 21:00
 */
public class ASyncBatchReadListener<T> extends AnalysisEventListener<T> {
    /**
     * 默认分批次大小
     */
    private static final int DEFAULT_BATCH_SIZE = 1000;

    private int batchSize;
    
    private BiConsumer<List<T>, AnalysisContext> consumer;
    
    private List<T> list = new ArrayList<>();

    public ASyncBatchReadListener(BiConsumer<List<T>, AnalysisContext> consumer) {
        batchSize = DEFAULT_BATCH_SIZE;
        this.consumer = consumer;
    }

    public ASyncBatchReadListener(int batchSize, BiConsumer<List<T>, AnalysisContext> consumer) {
        this.batchSize = batchSize;
        this.consumer = consumer;
    }

    @Override
    public void invoke(T object, AnalysisContext context) {
        list.add(object);
        if (list.size() >= batchSize) {
            consumer.accept(list, context);
            list.clear();
        }
    }
    
    @Override
    public void doAfterAllAnalysed(AnalysisContext context) {
        consumer.accept(list, context);
        list.clear();
    }

}
```

#### 写Excel

```java
@Data
public class DemoData {

    @ExcelProperty("姓名")
    private String name;

    @ExcelProperty("版本")
    private int version;

    @ExcelProperty("最后修改时间")
    @DateTimeFormat("yyyyMMdd")
    private Date lastModifiedTime;
}

@Test
public void write() {
    DemoData demoData = new DemoData();
    demoData.setName("测试写出");
    demoData.setVersion(1);
    demoData.setLastModifiedTime(new Date());
    EasyExcel.write("写出的Excel.xlsx", DemoData.class).sheet().doWrite(Lists.newArrayList(demoData));
}
```

### XXL-Excel

[https://mvnrepository.com/artifact/com.xuxueli/xxl-excel](https://mvnrepository.com/artifact/com.xuxueli/xxl-excel)

原项目已经停止维护，最新项目为`xxl-tool`

### XXL-Tool

[https://mvnrepository.com/artifact/com.xuxueli/xxl-tool](https://mvnrepository.com/artifact/com.xuxueli/xxl-tool)

### HuTool

[https://mvnrepository.com/artifact/cn.hutool/hutool-all](https://mvnrepository.com/artifact/cn.hutool/hutool-all)



## 邮件操作

### Java Mail

JavaMail是sum公司提供的一个使用Pop3、STMP、IMAP来收发邮件的开源库。

github:[https://github.com/javaee/javamail](https://github.com/javaee/javamail)

maven:[https://mvnrepository.com/artifact/javax.mail/mail](https://mvnrepository.com/artifact/javax.mail/mail)

```xml
<dependency>
    <groupId>javax.mail</groupId>
    <artifactId>mail</artifactId>
    <version>1.4.4</version>
</dependency>
```

Session

Message

Transport

Store

Folder

Address

```java
Folder defaultFolder = store.getDefaultFolder();
Folder[] allFolder = defaultFolder.list();
```

## 接口文档

### swagger-ui



### knife4j